node_modules
.next
out
.env
.env.local
.DS_Store
*.tsbuildinfo
next-env.d.ts

# Regenerated by `next dev` on every run (Next 16 `agentRules`). Local AI-agent
# scaffolding, not project content - ignored rather than committed so the tree
# stays clean without the repo carrying instructions nobody here wrote. Set
# `agentRules: false` in next.config.mjs to stop generating them entirely.
AGENTS.md
CLAUDE.md
